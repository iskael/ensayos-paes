# Grupos (profesor + estudiante) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir las pantallas de Grupos para profesor (mis grupos, detalle con progreso agregado y miembros, progreso individual de un estudiante) y estudiante (mis grupos + unirse por código), agregando primero un endpoint nuevo (`GET /grupos/mis-grupos`) que hoy no existe en el contrato.

**Architecture:** Extensión mínima de backend (un método de repo + un handler + una ruta + el schema en OpenAPI, siguiendo exactamente los patrones ya existentes en `internal/repo/grupos.go`/`internal/http/grupo_handler.go`) seguida de 4 páginas de frontend nuevas y chicas, cada una consumiendo endpoints ya existentes (o el nuevo) vía el cliente API.

**Tech Stack:** Mismo stack de las tandas anteriores (Go + chi + pgx en el backend; React 18, react-router-dom v7, CSS plano, `recharts` en el frontend). Sin dependencias nuevas en ningún lado.

## Global Constraints

- Sin dependencias nuevas (ni Go ni npm).
- Nombres de funciones/variables/componentes en español, siguiendo la convención ya usada.
- No reescribir archivos completos sin necesidad; ediciones parciales.
- El guard de rol vive en el backend (`RequerirRol`) — el frontend no lo duplica.
- Ningún método existente de `internal/repo/grupos.go` tiene test unitario (los tests del proyecto se concentran en `internal/domain`/`internal/pdfimport`, funciones puras sin DB) — `ListarPorEstudiante` sigue ese mismo criterio, sin test nuevo.
- Sin verificación en vivo con cuentas admin/profesor/estudiante reales por parte del controller (misma restricción de la tanda anterior — usar contraseñas reales o descartables en cualquier script/comando está bloqueado por la política de seguridad de este entorno). Verificación vía build limpio + auto-revisión de código línea por línea contra este plan y `docs/openapi.yaml`.
- Los ensayos NO están vinculados a un grupo — las estadísticas de un grupo se calculan agregando sobre los ensayos de sus miembros, no sobre una relación ensayo↔grupo (no existe tal relación).

---

### Task 1: Backend — endpoint `GET /grupos/mis-grupos`

**Files:**
- Modify: `backend/internal/repo/grupos.go`
- Modify: `backend/internal/http/grupo_handler.go`
- Modify: `backend/internal/http/router.go`
- Modify: `docs/openapi.yaml`

**Interfaces:**
- Consumes: `r.pool` (`*pgxpool.Pool`, ya existente en `Grupos`); `claimsDe(ctx)` (ya existente en `grupo_handler.go`, usado por `listar`/`unirse`); `escribirJSON`/`escribirError` (ya existentes).
- Produces:
  - `repo.GrupoConProfesor { ID, Nombre, ProfesorNombre string; FechaUnion time.Time }` (struct nueva, exportada).
  - `(*Grupos) ListarPorEstudiante(ctx context.Context, estudianteID string) ([]GrupoConProfesor, error)`.
  - `(*grupoHandler) misGrupos(w http.ResponseWriter, r *http.Request)`.
  - Ruta `GET /api/v1/grupos/mis-grupos` (rol estudiante).

- [ ] **Step 1: Agregar `GrupoConProfesor` y `ListarPorEstudiante` en `backend/internal/repo/grupos.go`**

Agregar al final del archivo (después de `EsMiembro`, no tocar nada anterior):

```go
type GrupoConProfesor struct {
	ID             string
	Nombre         string
	ProfesorNombre string
	FechaUnion     time.Time
}

// ListarPorEstudiante devuelve los grupos a los que pertenece un estudiante,
// con el nombre del profesor dueño, ordenados por fecha de unión (más
// reciente primero).
func (r *Grupos) ListarPorEstudiante(ctx context.Context, estudianteID string) ([]GrupoConProfesor, error) {
	const q = `SELECT g.id::text, g.nombre, u.nombre, gm.fecha_union
	           FROM grupo_miembros gm
	           JOIN grupos g ON g.id = gm.grupo_id
	           JOIN usuarios u ON u.id = g.profesor_id
	           WHERE gm.estudiante_id = $1
	           ORDER BY gm.fecha_union DESC`
	rows, err := r.pool.Query(ctx, q, estudianteID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	out := []GrupoConProfesor{}
	for rows.Next() {
		var g GrupoConProfesor
		if err := rows.Scan(&g.ID, &g.Nombre, &g.ProfesorNombre, &g.FechaUnion); err != nil {
			return nil, err
		}
		out = append(out, g)
	}
	return out, rows.Err()
}
```

- [ ] **Step 2: Agregar el handler `misGrupos` en `backend/internal/http/grupo_handler.go`**

Agregar al final del archivo:

```go
type grupoEstudianteResp struct {
	ID             string    `json:"id"`
	Nombre         string    `json:"nombre"`
	ProfesorNombre string    `json:"profesor_nombre"`
	FechaUnion     time.Time `json:"fecha_union"`
}

func (h *grupoHandler) misGrupos(w http.ResponseWriter, r *http.Request) {
	claims, _ := claimsDe(r.Context())
	lista, err := h.grupos.ListarPorEstudiante(r.Context(), claims.UsuarioID)
	if err != nil {
		escribirError(w, http.StatusInternalServerError, "INTERNO", "No se pudo listar los grupos")
		return
	}
	out := make([]grupoEstudianteResp, 0, len(lista))
	for _, g := range lista {
		out = append(out, grupoEstudianteResp{ID: g.ID, Nombre: g.Nombre, ProfesorNombre: g.ProfesorNombre, FechaUnion: g.FechaUnion})
	}
	escribirJSON(w, http.StatusOK, out)
}
```

- [ ] **Step 3: Agregar la ruta en `backend/internal/http/router.go`**

Dentro de `priv.Route("/grupos", func(gr chi.Router) { ... })`, agregar esta línea junto a la de `unirse` (no tocar el resto del bloque):

```go
gr.With(RequerirRol(domain.RolEstudiante)).Post("/unirse", grupoH.unirse)
gr.With(RequerirRol(domain.RolEstudiante)).Get("/mis-grupos", grupoH.misGrupos)
```

- [ ] **Step 4: Agregar el endpoint y schema en `docs/openapi.yaml`**

Agregar este path junto a los demás de Grupos (después de `/grupos/unirse`, antes de `/grupos/{grupoId}`):

```yaml
  /grupos/mis-grupos:
    get:
      tags: [Grupos]
      operationId: misGrupos
      summary: Listar los grupos del estudiante autenticado
      responses:
        '200':
          description: Grupos del estudiante
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/GrupoEstudiante' }
        '401': { $ref: '#/components/responses/NoAutorizado' }
        '403': { $ref: '#/components/responses/Prohibido' }
```

Agregar este schema junto a los demás de Grupos (después de `ProgresoEstudiante`):

```yaml
    GrupoEstudiante:
      type: object
      properties:
        id: { type: string, format: uuid }
        nombre: { type: string }
        profesor_nombre: { type: string }
        fecha_union: { type: string, format: date-time }
```

- [ ] **Step 5: Verificar que compila**

```bash
cd backend && go build ./... && go vet ./...
```
Expected: sin salida (ambos comandos terminan sin error).

- [ ] **Step 6: Validar el OpenAPI actualizado**

Si `openapi-spec-validator` está disponible (ya se usó en la Fase 7 del proyecto):

```bash
python3 -m openapi_spec_validator docs/openapi.yaml
```
Expected: sin errores. Si la herramienta no está instalada en este entorno, omitir este paso y decirlo en el reporte — no instalar dependencias nuevas para esto.

- [ ] **Step 7: Commit**

```bash
git add backend/internal/repo/grupos.go backend/internal/http/grupo_handler.go backend/internal/http/router.go docs/openapi.yaml
git commit -m "feat(backend): endpoint GET /grupos/mis-grupos para el estudiante"
git push origin main
```

---

### Task 2: Cliente API (`api.js`) — métodos de Grupos

**Files:**
- Modify: `frontend/src/api.js`

**Interfaces:**
- Consumes: `pedir()` (función interna ya existente, sin cambios necesarios — ninguno de estos métodos usa `FormData`).
- Produces:
  - `api.crearGrupo(token, body)` → `POST /api/v1/grupos` → `Promise<Grupo>`
  - `api.listarGrupos(token)` → `GET /api/v1/grupos` → `Promise<Grupo[]>`
  - `api.unirseGrupo(token, codigo)` → `POST /api/v1/grupos/unirse` → `Promise<Grupo>`
  - `api.obtenerGrupo(token, id)` → `GET /api/v1/grupos/:id` → `Promise<GrupoDetalle>`
  - `api.listarMiembros(token, id)` → `GET /api/v1/grupos/:id/miembros` → `Promise<MiembroGrupo[]>`
  - `api.progresoEstudiante(token, grupoId, estudianteId)` → `GET /api/v1/grupos/:grupoId/estudiantes/:estudianteId` → `Promise<ProgresoEstudiante>`
  - `api.misGrupos(token)` → `GET /api/v1/grupos/mis-grupos` → `Promise<GrupoEstudiante[]>`

- [ ] **Step 1: Agregar los métodos al objeto `api` en `frontend/src/api.js`**

Agregar dentro del objeto `export const api = { ... }` ya existente (junto a las demás entradas, no reescribir el archivo):

```js
  crearGrupo: (token, body) => pedir('/api/v1/grupos', { metodo: 'POST', body, token }),
  listarGrupos: (token) => pedir('/api/v1/grupos', { token }),
  unirseGrupo: (token, codigo) => pedir('/api/v1/grupos/unirse', { metodo: 'POST', body: { codigo }, token }),
  obtenerGrupo: (token, id) => pedir(`/api/v1/grupos/${id}`, { token }),
  listarMiembros: (token, id) => pedir(`/api/v1/grupos/${id}/miembros`, { token }),
  progresoEstudiante: (token, grupoId, estudianteId) =>
    pedir(`/api/v1/grupos/${grupoId}/estudiantes/${estudianteId}`, { token }),
  misGrupos: (token) => pedir('/api/v1/grupos/mis-grupos', { token }),
```

- [ ] **Step 2: Verificar con un script de Node (fetch mockeado, sin credenciales ni backend real)**

```bash
cd frontend && node -e '
import("vite").then(async ({ createServer }) => {
  const server = await createServer({ configFile: "vite.config.js", root: "." })
  try {
    const llamadas = []
    globalThis.fetch = async (url, opciones) => {
      llamadas.push({ url, opciones })
      return { status: 200, ok: true, json: async () => ({}) }
    }
    const { api } = await server.ssrLoadModule("/src/api.js")

    await api.crearGrupo("tok", { nombre: "Grupo A" })
    let l = llamadas.at(-1)
    console.log("crearGrupo POST /grupos:", l.opciones.method === "POST" && l.url.endsWith("/api/v1/grupos"))
    console.log("crearGrupo body:", l.opciones.body === JSON.stringify({ nombre: "Grupo A" }))

    await api.listarGrupos("tok")
    l = llamadas.at(-1)
    console.log("listarGrupos GET /grupos:", l.opciones.method === "GET" && l.url.endsWith("/api/v1/grupos"))

    await api.unirseGrupo("tok", "ABC1234")
    l = llamadas.at(-1)
    console.log("unirseGrupo POST /grupos/unirse:", l.opciones.method === "POST" && l.url.endsWith("/api/v1/grupos/unirse"))
    console.log("unirseGrupo body { codigo }:", l.opciones.body === JSON.stringify({ codigo: "ABC1234" }))

    await api.obtenerGrupo("tok", "g1")
    l = llamadas.at(-1)
    console.log("obtenerGrupo GET /grupos/g1:", l.url.endsWith("/api/v1/grupos/g1"))

    await api.listarMiembros("tok", "g1")
    l = llamadas.at(-1)
    console.log("listarMiembros GET /grupos/g1/miembros:", l.url.endsWith("/api/v1/grupos/g1/miembros"))

    await api.progresoEstudiante("tok", "g1", "e1")
    l = llamadas.at(-1)
    console.log("progresoEstudiante GET .../g1/estudiantes/e1:", l.url.endsWith("/api/v1/grupos/g1/estudiantes/e1"))

    await api.misGrupos("tok")
    l = llamadas.at(-1)
    console.log("misGrupos GET /grupos/mis-grupos:", l.url.endsWith("/api/v1/grupos/mis-grupos"))
  } finally {
    await server.close()
  }
})
'
```
Expected: `true` en las 8 líneas. (Si `import.meta.env` da problemas fuera de este workaround de Vite, ya está resuelto usando `server.ssrLoadModule` en vez de `import()` directo sobre `api.js`.)

- [ ] **Step 3: Commit**

```bash
git add frontend/src/api.js
git commit -m "feat(frontend): cliente API de grupos"
git push origin main
```

---

### Task 3: Activar menú de profesor y agregar "Mis grupos" al de estudiante

**Files:**
- Modify: `frontend/src/constantes.js`

**Interfaces:**
- Consumes: nada nuevo.
- Produces: `MENU_POR_ROL.profesor` con `disponible: true, ruta: '/grupos'`; `MENU_POR_ROL.estudiante` con una tercera entrada `{ etiqueta: 'Mis grupos', ruta: '/mis-grupos', disponible: true }`.

- [ ] **Step 1: Editar `MENU_POR_ROL` en `frontend/src/constantes.js`**

Reemplazar el objeto completo (mismo contenido, solo cambian las entradas de `estudiante` y `profesor`; `admin` no se toca):

```js
export const MENU_POR_ROL = {
  estudiante: [
    { etiqueta: 'Configurar ensayo', ruta: '/', disponible: true },
    { etiqueta: 'Mi progreso', ruta: '/dashboard', disponible: true },
    { etiqueta: 'Mis grupos', ruta: '/mis-grupos', disponible: true },
  ],
  profesor: [{ etiqueta: 'Mis grupos', ruta: '/grupos', disponible: true }],
  admin: [{ etiqueta: 'Banco de preguntas', ruta: '/banco/items', disponible: true }],
}
```

- [ ] **Step 2: Verificar con un script de Node**

```bash
cd frontend && node -e '
import("./src/constantes.js").then(({ MENU_POR_ROL }) => {
  console.log("estudiante tiene 3 entradas:", MENU_POR_ROL.estudiante.length === 3)
  console.log("estudiante Mis grupos ok:", MENU_POR_ROL.estudiante[2].ruta === "/mis-grupos" && MENU_POR_ROL.estudiante[2].disponible === true)
  console.log("profesor Mis grupos activo:", MENU_POR_ROL.profesor[0].ruta === "/grupos" && MENU_POR_ROL.profesor[0].disponible === true)
  console.log("admin sin cambios:", MENU_POR_ROL.admin[0].ruta === "/banco/items")
})
'
```
Expected: `true` cuatro veces.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/constantes.js
git commit -m "feat(frontend): activa el menu de grupos para profesor y estudiante"
git push origin main
```

---

### Task 4: `MisGruposProfesor.jsx` (lista + crear inline) + ruta

**Files:**
- Create: `frontend/src/pages/MisGruposProfesor.jsx`
- Modify: `frontend/src/App.jsx` (nueva ruta `/grupos`)

**Interfaces:**
- Consumes: `api.listarGrupos`, `api.crearGrupo` (Task 2); `useApi()`.
- Produces: `<MisGruposProfesor />` (componente de página, default export), montado en `/grupos`.

- [ ] **Step 1: Escribir `frontend/src/pages/MisGruposProfesor.jsx`**

```jsx
import { useState, useEffect } from 'react'
import { Link } from 'react-router-dom'
import { api, ApiError } from '../api.js'
import { useApi } from '../useApi.js'

export default function MisGruposProfesor() {
  const { llamar } = useApi()
  const [grupos, setGrupos] = useState(null)
  const [error, setError] = useState(null)
  const [nombreNuevo, setNombreNuevo] = useState('')
  const [creando, setCreando] = useState(false)
  const [errorCrear, setErrorCrear] = useState(null)

  useEffect(() => {
    let cancelado = false
    llamar((token) => api.listarGrupos(token))
      .then((lista) => {
        if (!cancelado) setGrupos(lista)
      })
      .catch(() => {
        if (!cancelado) setError('No se pudieron cargar los grupos')
      })
    return () => {
      cancelado = true
    }
  }, [llamar])

  async function crearGrupo(evento) {
    evento.preventDefault()
    if (!nombreNuevo.trim()) return
    setErrorCrear(null)
    setCreando(true)
    try {
      const grupo = await llamar((token) => api.crearGrupo(token, { nombre: nombreNuevo.trim() }))
      setGrupos((actuales) => [grupo, ...(actuales ?? [])])
      setNombreNuevo('')
    } catch (err) {
      setErrorCrear(
        err instanceof ApiError ? err.mensaje || 'No se pudo crear el grupo' : 'No se pudo conectar con el servidor',
      )
    } finally {
      setCreando(false)
    }
  }

  if (error) {
    return (
      <div className="pantalla">
        <div className="tarjeta">
          <p className="error">{error}</p>
        </div>
      </div>
    )
  }

  if (!grupos) {
    return (
      <div className="pantalla">
        <div className="tarjeta">Cargando…</div>
      </div>
    )
  }

  return (
    <div className="pantalla">
      <div className="tarjeta tarjeta-ancha">
        <h1>Mis grupos</h1>

        <form onSubmit={crearGrupo} style={{ display: 'flex', gap: 8, marginBottom: 16 }}>
          <input
            type="text"
            value={nombreNuevo}
            onChange={(e) => setNombreNuevo(e.target.value)}
            placeholder="Nombre del grupo"
            style={{
              flex: 1,
              minHeight: 44,
              padding: '0 12px',
              border: '1px solid var(--color-borde)',
              borderRadius: 8,
            }}
          />
          <button className="boton" type="submit" disabled={creando} style={{ width: 'auto', padding: '0 16px' }}>
            {creando ? 'Creando…' : 'Crear grupo'}
          </button>
        </form>
        {errorCrear && <p className="error">{errorCrear}</p>}

        {grupos.length === 0 ? (
          <p>Todavía no creaste ningún grupo.</p>
        ) : (
          <table className="banco-tabla">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>Código</th>
                <th>Creado</th>
              </tr>
            </thead>
            <tbody>
              {grupos.map((g) => (
                <tr key={g.id}>
                  <td>
                    <Link to={`/grupos/${g.id}`}>{g.nombre}</Link>
                  </td>
                  <td>{g.codigo_invitacion}</td>
                  <td>{new Date(g.fecha_creacion).toLocaleDateString()}</td>
                </tr>
              ))}
            </tbody>
          </table>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Agregar la ruta `/grupos` en `frontend/src/App.jsx`**

Agregar el import:

```jsx
import MisGruposProfesor from './pages/MisGruposProfesor.jsx'
```

Agregar la ruta (junto a las demás rutas privadas):

```jsx
<Route
  path="/grupos"
  element={
    <RutaPrivada>
      <LayoutAutenticado>
        <MisGruposProfesor />
      </LayoutAutenticado>
    </RutaPrivada>
  }
/>
```

- [ ] **Step 3: Verificar con build + auto-revisión**

```bash
cd frontend && npm run build
```
Expected: build limpio, sin errores.

Releer el diff propio contra este Step 1 y confirmar: ¿el formulario de creación no envía si el nombre está vacío (`if (!nombreNuevo.trim()) return`)? ¿el grupo recién creado se agrega al principio de la lista local sin recargar toda la pantalla? ¿la tabla usa las clases ya existentes `tarjeta-ancha`/`banco-tabla` (definidas en la tanda de Banco de preguntas, `frontend/src/styles.css`)?

Si tenés una herramienta de navegador disponible (Claude Preview o claude-in-chrome), podés opcionalmente cargar `/grupos` con una sesión de estudiante/no-profesor descartable (mismo patrón ya usado en la tanda anterior — sin credenciales de profesor reales) solo para confirmar que la pantalla no crashea (el backend rechazará con 403, es esperado).

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/MisGruposProfesor.jsx frontend/src/App.jsx
git commit -m "feat(frontend): lista de grupos del profesor con creacion inline"
git push origin main
```

---

### Task 5: `GrupoDetalle.jsx` (progreso agregado + miembros) + ruta

**Files:**
- Create: `frontend/src/pages/GrupoDetalle.jsx`
- Modify: `frontend/src/App.jsx` (nueva ruta `/grupos/:id`)

**Interfaces:**
- Consumes: `api.obtenerGrupo`, `api.listarMiembros` (Task 2); `etiquetaEje` (ya existente en `constantes.js`); `useApi()`.
- Produces: `<GrupoDetalle />` (componente de página, default export), montado en `/grupos/:id`.

- [ ] **Step 1: Escribir `frontend/src/pages/GrupoDetalle.jsx`**

```jsx
import { useState, useEffect } from 'react'
import { useParams, Link } from 'react-router-dom'
import { api } from '../api.js'
import { useApi } from '../useApi.js'
import { etiquetaEje } from '../constantes.js'

export default function GrupoDetalle() {
  const { id } = useParams()
  const { llamar } = useApi()
  const [grupo, setGrupo] = useState(null)
  const [miembros, setMiembros] = useState(null)
  const [error, setError] = useState(null)

  useEffect(() => {
    let cancelado = false
    Promise.all([llamar((token) => api.obtenerGrupo(token, id)), llamar((token) => api.listarMiembros(token, id))])
      .then(([g, m]) => {
        if (cancelado) return
        setGrupo(g)
        setMiembros(m)
      })
      .catch(() => {
        if (!cancelado) setError('No se pudo cargar el grupo')
      })
    return () => {
      cancelado = true
    }
  }, [id, llamar])

  if (error) {
    return (
      <div className="pantalla">
        <div className="tarjeta">
          <p className="error">{error}</p>
        </div>
      </div>
    )
  }

  if (!grupo || !miembros) {
    return (
      <div className="pantalla">
        <div className="tarjeta">Cargando…</div>
      </div>
    )
  }

  return (
    <div className="pantalla">
      <div className="tarjeta tarjeta-ancha">
        <h1>{grupo.nombre}</h1>
        <p>
          Código de invitación: <strong>{grupo.codigo_invitacion}</strong>
        </p>
        <p>
          {grupo.cantidad_miembros} estudiante{grupo.cantidad_miembros === 1 ? '' : 's'}
        </p>
        <p>Promedio del grupo: {grupo.promedio_grupo == null ? '—' : Math.round(grupo.promedio_grupo)}</p>

        <h2>Desempeño por eje</h2>
        <ul>
          {grupo.desempeno_por_eje.map((d) => (
            <li key={d.eje}>
              {etiquetaEje(d.eje)}: {d.correctas}/{d.total}
            </li>
          ))}
        </ul>

        <h2>Miembros</h2>
        {miembros.length === 0 ? (
          <p>Todavía no se unió ningún estudiante.</p>
        ) : (
          <table className="banco-tabla">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>Unión</th>
                <th>Ensayos</th>
                <th>Último puntaje</th>
              </tr>
            </thead>
            <tbody>
              {miembros.map((m) => (
                <tr key={m.estudiante_id}>
                  <td>
                    <Link to={`/grupos/${id}/estudiantes/${m.estudiante_id}`}>{m.nombre}</Link>
                  </td>
                  <td>{new Date(m.fecha_union).toLocaleDateString()}</td>
                  <td>{m.total_ensayos}</td>
                  <td>{m.ultimo_puntaje == null ? '—' : m.ultimo_puntaje}</td>
                </tr>
              ))}
            </tbody>
          </table>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Agregar la ruta `/grupos/:id` en `frontend/src/App.jsx`**

Agregar el import:

```jsx
import GrupoDetalle from './pages/GrupoDetalle.jsx'
```

Agregar la ruta:

```jsx
<Route
  path="/grupos/:id"
  element={
    <RutaPrivada>
      <LayoutAutenticado>
        <GrupoDetalle />
      </LayoutAutenticado>
    </RutaPrivada>
  }
/>
```

- [ ] **Step 3: Verificar con build + auto-revisión**

```bash
cd frontend && npm run build
```
Expected: build limpio.

Releer el diff propio contra el Step 1: ¿`promedio_grupo == null` se muestra como "—" en vez de "null" o "NaN"? ¿`ultimo_puntaje == null` por fila también se muestra como "—"? ¿el link de cada miembro apunta a `/grupos/${id}/estudiantes/${m.estudiante_id}` (la ruta que se agrega en la Tarea 6)?

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/GrupoDetalle.jsx frontend/src/App.jsx
git commit -m "feat(frontend): detalle de grupo con progreso agregado y miembros"
git push origin main
```

---

### Task 6: `ProgresoEstudianteGrupo.jsx` (evolución individual) + ruta

**Files:**
- Create: `frontend/src/pages/ProgresoEstudianteGrupo.jsx`
- Modify: `frontend/src/App.jsx` (nueva ruta `/grupos/:id/estudiantes/:estudianteId`)

**Interfaces:**
- Consumes: `api.progresoEstudiante` (Task 2); `etiquetaEje` (ya existente); `useApi()`; `recharts` (ya instalado, usado por `Dashboard.jsx`).
- Produces: `<ProgresoEstudianteGrupo />` (componente de página, default export), montado en `/grupos/:id/estudiantes/:estudianteId`.

- [ ] **Step 1: Escribir `frontend/src/pages/ProgresoEstudianteGrupo.jsx`**

```jsx
import { useState, useEffect } from 'react'
import { useParams } from 'react-router-dom'
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
import { api } from '../api.js'
import { useApi } from '../useApi.js'
import { etiquetaEje } from '../constantes.js'

function formatearFecha(fechaIso) {
  const d = new Date(fechaIso)
  return `${String(d.getDate()).padStart(2, '0')}/${String(d.getMonth() + 1).padStart(2, '0')}`
}

export default function ProgresoEstudianteGrupo() {
  const { id, estudianteId } = useParams()
  const { llamar } = useApi()
  const [progreso, setProgreso] = useState(null)
  const [error, setError] = useState(null)

  useEffect(() => {
    let cancelado = false
    llamar((token) => api.progresoEstudiante(token, id, estudianteId))
      .then((p) => {
        if (!cancelado) setProgreso(p)
      })
      .catch(() => {
        if (!cancelado) setError('No se pudo cargar el progreso del estudiante')
      })
    return () => {
      cancelado = true
    }
  }, [id, estudianteId, llamar])

  if (error) {
    return (
      <div className="pantalla">
        <div className="tarjeta">
          <p className="error">{error}</p>
        </div>
      </div>
    )
  }

  if (!progreso) {
    return (
      <div className="pantalla">
        <div className="tarjeta">Cargando…</div>
      </div>
    )
  }

  const datosGrafico = progreso.evolucion.map((p) => ({ fecha: formatearFecha(p.fecha), puntaje: p.puntaje }))

  return (
    <div className="pantalla">
      <div className="tarjeta tarjeta-ancha">
        <h1>{progreso.estudiante.nombre}</h1>
        <p>
          {progreso.estudiante.total_ensayos} ensayos rendidos — último puntaje:{' '}
          {progreso.estudiante.ultimo_puntaje == null ? '—' : progreso.estudiante.ultimo_puntaje}
        </p>

        <h2>Desempeño por eje</h2>
        <ul>
          {progreso.desempeno_por_eje.map((d) => (
            <li key={d.eje}>
              {etiquetaEje(d.eje)}: {d.correctas}/{d.total}
            </li>
          ))}
        </ul>

        {datosGrafico.length > 0 && (
          <>
            <h2>Evolución del puntaje</h2>
            <ResponsiveContainer width="100%" height={220}>
              <LineChart data={datosGrafico}>
                <CartesianGrid strokeDasharray="3 3" />
                <XAxis dataKey="fecha" />
                <YAxis domain={[0, 1000]} />
                <Tooltip />
                <Line type="monotone" dataKey="puntaje" stroke="#2f6fed" strokeWidth={2} dot={{ r: 3 }} />
              </LineChart>
            </ResponsiveContainer>
          </>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Agregar la ruta en `frontend/src/App.jsx`**

Agregar el import:

```jsx
import ProgresoEstudianteGrupo from './pages/ProgresoEstudianteGrupo.jsx'
```

Agregar la ruta:

```jsx
<Route
  path="/grupos/:id/estudiantes/:estudianteId"
  element={
    <RutaPrivada>
      <LayoutAutenticado>
        <ProgresoEstudianteGrupo />
      </LayoutAutenticado>
    </RutaPrivada>
  }
/>
```

- [ ] **Step 3: Verificar con build + auto-revisión**

```bash
cd frontend && npm run build
```
Expected: build limpio.

Releer el diff propio: ¿el gráfico se omite completamente (no solo queda vacío) cuando `progreso.evolucion` tiene longitud 0, igual que en `Dashboard.jsx`? ¿`useParams()` extrae correctamente `id` y `estudianteId` de la ruta `/grupos/:id/estudiantes/:estudianteId` (dos parámetros, no uno)?

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/ProgresoEstudianteGrupo.jsx frontend/src/App.jsx
git commit -m "feat(frontend): progreso individual de un estudiante dentro de un grupo"
git push origin main
```

---

### Task 7: `MisGruposEstudiante.jsx` (lista + unirse por código) + ruta

**Files:**
- Create: `frontend/src/pages/MisGruposEstudiante.jsx`
- Modify: `frontend/src/App.jsx` (nueva ruta `/mis-grupos`)

**Interfaces:**
- Consumes: `api.misGrupos`, `api.unirseGrupo` (Task 2); `useApi()`.
- Produces: `<MisGruposEstudiante />` (componente de página, default export), montado en `/mis-grupos`.

- [ ] **Step 1: Escribir `frontend/src/pages/MisGruposEstudiante.jsx`**

```jsx
import { useState, useEffect } from 'react'
import { api, ApiError } from '../api.js'
import { useApi } from '../useApi.js'

export default function MisGruposEstudiante() {
  const { llamar } = useApi()
  const [grupos, setGrupos] = useState(null)
  const [error, setError] = useState(null)
  const [codigo, setCodigo] = useState('')
  const [uniendo, setUniendo] = useState(false)
  const [errorUnirse, setErrorUnirse] = useState(null)

  useEffect(() => {
    let cancelado = false
    llamar((token) => api.misGrupos(token))
      .then((lista) => {
        if (!cancelado) setGrupos(lista)
      })
      .catch(() => {
        if (!cancelado) setError('No se pudieron cargar tus grupos')
      })
    return () => {
      cancelado = true
    }
  }, [llamar])

  async function unirse(evento) {
    evento.preventDefault()
    if (!codigo.trim()) return
    setErrorUnirse(null)
    setUniendo(true)
    try {
      await llamar((token) => api.unirseGrupo(token, codigo.trim().toUpperCase()))
      const lista = await llamar((token) => api.misGrupos(token))
      setGrupos(lista)
      setCodigo('')
    } catch (err) {
      setErrorUnirse(
        err instanceof ApiError ? err.mensaje || 'No se pudo unir al grupo' : 'No se pudo conectar con el servidor',
      )
    } finally {
      setUniendo(false)
    }
  }

  if (error) {
    return (
      <div className="pantalla">
        <div className="tarjeta">
          <p className="error">{error}</p>
        </div>
      </div>
    )
  }

  if (!grupos) {
    return (
      <div className="pantalla">
        <div className="tarjeta">Cargando…</div>
      </div>
    )
  }

  return (
    <div className="pantalla">
      <div className="tarjeta">
        <h1>Mis grupos</h1>

        <form onSubmit={unirse} style={{ display: 'flex', gap: 8, marginBottom: 16 }}>
          <input
            type="text"
            value={codigo}
            onChange={(e) => setCodigo(e.target.value)}
            placeholder="Código de invitación"
            style={{
              flex: 1,
              minHeight: 44,
              padding: '0 12px',
              border: '1px solid var(--color-borde)',
              borderRadius: 8,
            }}
          />
          <button className="boton" type="submit" disabled={uniendo} style={{ width: 'auto', padding: '0 16px' }}>
            {uniendo ? 'Uniendo…' : 'Unirme'}
          </button>
        </form>
        {errorUnirse && <p className="error">{errorUnirse}</p>}

        {grupos.length === 0 ? (
          <p>Todavía no perteneces a ningún grupo. Pedile el código a tu profesor.</p>
        ) : (
          <ul>
            {grupos.map((g) => (
              <li key={g.id}>
                <strong>{g.nombre}</strong> — profesor {g.profesor_nombre} (desde{' '}
                {new Date(g.fecha_union).toLocaleDateString()})
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Agregar la ruta `/mis-grupos` en `frontend/src/App.jsx`**

Agregar el import:

```jsx
import MisGruposEstudiante from './pages/MisGruposEstudiante.jsx'
```

Agregar la ruta:

```jsx
<Route
  path="/mis-grupos"
  element={
    <RutaPrivada>
      <LayoutAutenticado>
        <MisGruposEstudiante />
      </LayoutAutenticado>
    </RutaPrivada>
  }
/>
```

- [ ] **Step 3: Verificar con build + auto-revisión, y en vivo con un estudiante descartable**

```bash
cd frontend && npm run build
```
Expected: build limpio.

Esta es la única pantalla de esta tanda que un estudiante puede probar en vivo sin ninguna restricción (el registro público de estudiantes no requiere credenciales de nadie más — mismo patrón ya usado en tandas anteriores). Si tenés `npm run dev` corriendo contra el backend real (`frontend/.env.development` ya apunta a `http://192.168.0.190:8080`):

1. Registrar un estudiante nuevo vía `/registro`.
2. Ir a `/mis-grupos` (directo por URL o desde el drawer del menú) y confirmar el mensaje de "todavía no pertenecés a ningún grupo".
3. Intentar unirse con un código inventado (ej. `ZZZZZZZ`) y confirmar que se muestra "Código inválido" sin romper la pantalla.

No hace falta un código real para esta verificación (no hay forma de generarlo sin loguearse como profesor, fuera de alcance de esta sesión) — el caso de "código inválido" ya ejercita el path de error real contra el backend desplegado.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/MisGruposEstudiante.jsx frontend/src/App.jsx
git commit -m "feat(frontend): mis grupos del estudiante y unirse por codigo"
git push origin main
```

---

### Task 8: Redirección de `/` también para profesor

**Files:**
- Modify: `frontend/src/App.jsx`

**Interfaces:**
- Consumes: `InicioPorRol` (ya existente, de la tanda de Banco de preguntas).
- Produces: `InicioPorRol` con una rama nueva para `profesor`.

- [ ] **Step 1: Editar `InicioPorRol` en `frontend/src/App.jsx`**

Reemplazar la función completa (agrega una rama, no cambia las demás):

```jsx
function InicioPorRol() {
  const { usuario } = useAuth()
  if (usuario?.rol === 'admin') return <Navigate to="/banco/items" replace />
  if (usuario?.rol === 'profesor') return <Navigate to="/grupos" replace />
  return <ConfigurarEnsayo />
}
```

- [ ] **Step 2: Verificar con build + auto-revisión, y en vivo con un estudiante descartable**

```bash
cd frontend && npm run build
```
Expected: build limpio.

Con `npm run dev`, registrar (o reusar) un estudiante y confirmar que sigue cayendo en "Configurar ensayo" en `/`, sin cambios de comportamiento (la rama nueva es para `profesor`, que no se puede probar en vivo en este entorno — confirmar por lectura de código que el `if` está bien ubicado antes del `return <ConfigurarEnsayo />` final).

- [ ] **Step 3: Commit**

```bash
git add frontend/src/App.jsx
git commit -m "feat(frontend): redirige / a /grupos para el rol profesor"
git push origin main
```

---

### Task 9: Desplegar en Proxmox

**Files:** ninguno (solo comandos de despliegue).

**Interfaces:** Consumes: todo lo anterior (backend con el endpoint nuevo + build de producción del frontend con las 4 pantallas de grupos incluidas).

- [ ] **Step 1: Redesplegar backend y frontend en el CT 118 de Proxmox**

```bash
# vía SSH al host 192.168.0.155 -> pct exec 118:
cd /opt/app && git pull && docker compose up -d --build api web
```

- [ ] **Step 2: Verificar en vivo contra el despliegue real**

Abrir `http://192.168.0.190/` (y `http://100.93.161.84/` vía Tailscale). Con un estudiante descartable: ir a "Mis grupos" desde el menú, confirmar que carga (vacío o con el mensaje de "todavía no pertenecés a ningún grupo"), e intentar unirse con un código inventado para confirmar el mensaje de error. La verificación completa del lado profesor (crear grupo, ver progreso agregado, progreso individual) queda pendiente para que la hagas vos manualmente con una cuenta de profesor real.
